import react from 'react';

const SubTable = (props) => {
    return (
        <div className={props.className} >
            {
                <div className='w-[100%] h-[30px] border-[1px] bg-white ' >

                </div>
            }
        </div>
    );
};

export default SubTable